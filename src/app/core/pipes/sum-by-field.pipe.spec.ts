import { SumByFieldPipe } from './sum-by-field.pipe';

describe('SumByFieldPipe', () => {
  it('create an instance', () => {
    const pipe = new SumByFieldPipe();
    expect(pipe).toBeTruthy();
  });
});
